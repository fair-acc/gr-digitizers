module: digitizers
block: block_demux
label: block_demux
blocktype: sync_block

typekeys:
  - id: T
    type: class
    options:
        - rf32

parameters:
-   id: bit_to_keep
    label: Bit to Keep
    dtype: size

ports:
-   domain: stream
    id: in
    direction: input
    type: ri8

-   domain: stream
    id: out
    direction: output
    type: typekeys/T

implementations:
-   id: cpu

file_format: 1
