module: digitizers
block: wr_receiver
label: wr_receiver
blocktype: sync_block

typekeys:
  - id: T
    type: class
    options:
        - rf32

callbacks:
    - id: add_timing_event
      return: bool
      args:
      - id: event_id
        dtype: string
      - id: wr_trigger_stamp
        dtype: ri64
      - id: wr_trigger_stamp_utc
        dtype: ri64

ports:
-   domain: stream
    id: out
    direction: output
    type: typekeys/T

implementations:
-   id: cpu

file_format: 1
